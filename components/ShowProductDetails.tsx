const ShowProductDetails = ({ productId }: { productId: string }) => {
  console.log(productId);

  return (
    <div className="w-full h-auto">ShowProductDetails</div>
  )
}

export default ShowProductDetails